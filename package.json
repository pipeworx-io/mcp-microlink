{
  "name": "@pipeworx/mcp-microlink",
  "version": "0.1.0",
  "description": "MCP server for URL metadata extraction and screenshots via Microlink API",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "keywords": ["mcp", "mcp-server", "model-context-protocol", "pipeworx", "microlink", "metadata", "screenshot"],
  "author": "Pipeworx",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
